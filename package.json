{
  "name": "tavern-bot",
  "version": "1.0.0",
  "description": "Lineage 2 instance zone tracker for Telegram chat",
  "private": true,
  "workspaces": [
    "backend",
    "frontend",
    "telegram-bot"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=backend\" \"npm run dev --workspace=frontend\"",
    "install:all": "npm install && npm install --workspace=backend && npm install --workspace=frontend && npm install --workspace=telegram-bot"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
